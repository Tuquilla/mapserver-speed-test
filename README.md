# mapserver-speed-test

## Usage
Add txt file with WFS Requests (one line, one request URL) as argument to the execution of the binary.

e.g.
```
mapserver-speed-test.exe input_file.txt
```
The tool measures the time the mapserver takes to answer the request.